meta {
  name: Delete Task
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/tasks/{{taskId}}
  body: none
  auth: inherit
}

tests {
  test("status is 204", function () {
    expect(res.status).to.equal(204);
  });
}
