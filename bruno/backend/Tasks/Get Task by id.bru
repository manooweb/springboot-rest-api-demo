meta {
  name: Get Task by id
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/tasks/{{taskId}}
  body: none
  auth: inherit
}

tests {
  test("status is 200", function () {
    expect(res.status).to.equal(200);
  });
}
