meta {
  name: Delete Task
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/v1/tasks/{{taskId}}
  auth: inherit
}

tests {
  test("status is 204", function () {
    expect(res.status).to.equal(204);
  });
}
