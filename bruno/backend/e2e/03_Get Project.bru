meta {
  name: Get Project
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/projects/{{projectId}}
  body: none
  auth: inherit
}

tests {
  test("status is 200", function () {
    expect(res.status).to.equal(200);
  });
  
  test("id matches projectId", function () {
    expect(res.body.id).to.equal(bru.getVar("projectId"));
  });
}
