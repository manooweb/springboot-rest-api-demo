meta {
  name: Delete Project
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/v1/projects/{{projectId}}
  body: none
  auth: none
}

tests {
  test("status is 204", function () {
    expect(res.status).to.equal(204);
  });
}
